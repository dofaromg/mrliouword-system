name: Mrliouword SDK CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install flake8 black mypy
          pip install -r requirements.txt

      - name: Lint with flake8
        run: |
          flake8 mrliouword_agents/ --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 mrliouword_agents/ --count --exit-zero --max-complexity=10 --max-line-length=88 --statistics

      - name: Check formatting with black
        run: black --check mrliouword_agents/

      - name: Type check with mypy
        run: mypy mrliouword_agents/ --ignore-missing-imports

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10", "3.11"]
    
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements-test.txt
          pip install -e .

      - name: Run tests
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: pytest --cov=mrliouword_agents --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: unittests-${{ matrix.python-version }}

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install bandit safety

      - name: Run Bandit Security Scan
        run: bandit -r mrliouword_agents/ -ll

      - name: Run Safety Check
        run: safety check --json || true

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install build tools
        run: |
          pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check package
        run: twine check dist/*

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: dist
          path: dist/

  provenance-notice:
    name: MRL 來源標註檢查
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      # 依權利人文件〈補正程序〉第 4 項：
      # 「技術門檻：MRL canonical 加入 provenance 文件與 CI 檢查」。
      # 只用標準函式庫，不裝任何套件——這道檢查不該因為安裝失敗而失效。
      - name: 檢查 MRL_PROVENANCE.md 九個規格欄位
        run: python tools/provenance_notice_check.py

      - name: 摘要
        if: always()
        run: |
          {
            echo "### MRL 來源標註檢查"
            echo ""
            echo "規格來源：\`Mrliou_MRL_GitHub_Payment_Provenance_Incident_Record_20260914_v3\`"
            echo ""
            python tools/provenance_notice_check.py 2>&1 | sed 's/^/    /'
          } >> "$GITHUB_STEP_SUMMARY"
