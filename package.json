{
  "name": "mrliouword-private",
  "version": "2.1.0",
  "description": "MrliouWord Private AI Server with Vector Attention Engine",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "versions:upload": "wrangler versions upload --config wrangler.jsonc",
    "build:containers": "npm --prefix containers run build",
    "deploy:particle-api": "wrangler deploy --config cloudflare/particle-api/wrangler.jsonc",
    "test": "bash test.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "typescript": "^5.3.0",
    "wrangler": "^4.59.2"
  }
}
